#pragma once

#include <string>

const std::string OUT_DIR_RESULTS = "out/results/json";
// It seems impossible in C++
// std::string OUT_FILE_RESULT_FMT_JSON = "{n:06}_{r:06}_{t:03}.json";
const std::string LOG_DIR = "logs/";
const std::string LOG_FILE_DEFAULT = LOG_DIR + "default.log";
